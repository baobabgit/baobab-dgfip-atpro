# Context — BL-008

Detecter le type de fichier et la variante de schema a partir des colonnes
normalisees. Le nom de fichier n'est qu'un indice faible, jamais le seul critere.

Dependance : BL-007 (HeaderNormalizer / colonnes normalisees).
