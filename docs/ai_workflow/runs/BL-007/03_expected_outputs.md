# Expected outputs — BL-007

- Module `src/atpro/parser/detection/`
- `FileInspection` + `FileInspector`
- SHA-256 streaming, détection encodage/séparateur, en-têtes normalisés
- Tests : UTF-8, Windows-1252, `;`, vide, absent
- Gates locales vertes ; PR vers `version/v0.1.0`
