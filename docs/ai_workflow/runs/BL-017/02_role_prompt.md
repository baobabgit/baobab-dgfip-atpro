# Role prompt — BL-017

Role : **Developpeur Python**

Implementer le CLI Typer `atpro` (groupe `file`), presenter, codes de sortie,
tests CliRunner, dependance `typer`, entry point `[project.scripts]`.
Passer les gates qualite sans commit.
