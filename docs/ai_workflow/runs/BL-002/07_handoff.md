# Handoff — BL-002

## Etat

MERGED vers `version/v0.1.0` (PR #10, squash).

## Fait

- Remplacement de `example_package` par `atpro`.
- Mise à jour `pyproject.toml`, Sphinx, release workflow, README, `uv.lock`.

## Suite

Démarrer BL-003 (arborescence domaine).
