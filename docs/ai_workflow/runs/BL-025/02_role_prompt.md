# Role prompt — Developpeur Python

Implementer `atpro.infrastructure.database` selon BL-025 / FEAT-016.1.

Contraintes :

- SQLAlchemy 2.x + typage strict ;
- 1 classe = 1 fichier autant que possible ;
- livrables `base.py` et `session.py` ;
- tests miroir sous `tests/unit/atpro/infrastructure/database/` ;
- couverture globale ≥ 95 %.
