# Worklog — BL-027

## 2026-07-26

- `alembic init`, env.py branche sur settings/Base, baseline vide.
- `AlembicConfigFactory`, Makefile `db-upgrade`, doc migrations.
- Tests : config, metadata, upgrade head + downgrade base (SQLite).
- Gates : 350 passed, cov 97.54 %.
