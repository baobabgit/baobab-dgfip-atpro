# Context — BL-028

Apres Alembic baseline (BL-027), creer le schema imports + referentiels
(sites, agents, alias, affectations, import_batches, import_rejected_rows).
Tables metier reportees a BL-029. Repositories reportes a BL-030+.
