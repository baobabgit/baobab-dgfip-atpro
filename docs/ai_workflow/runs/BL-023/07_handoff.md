# Handoff — BL-023

## Etat

- PR #41 mergee sur `version/v0.2.0`.
- Queue `BL-023` = DONE.
- Run cloture (`status: MERGED`).

## Suite

**BL-024** — PostgreSQL via Docker Compose (`compose.yml` + doc operations).
