# Context — BL-018

Dependances satisfaites (BL-008 schemas). Readers et `ParseFileUseCase` deja
disponibles (BL-012 a BL-016). Objectif : rendre la CI autonome sans fichiers
CSV reels / PII.

Personnages fictifs : Alice DUPONT, Bob MARTIN, sites Paris/Lyon/Lille,
numeros synthetiques `0611111111`, `0142000000`.
