# Handoff — BL-006

## Etat

MERGED vers `version/v0.1.0` (PR #14, squash).

## Fait

- Contrat uniforme de diagnostic sous `atpro.parser.results`.
- Export `parser` depuis le package `atpro`.
- Bookkeeping post-merge synchronisé (queue DONE, status MERGED, revue/handoff).

## Suite

Prochain item exécutable : **BL-007** (détection fichier).
Mettre `BL-007` en `IN_PROGRESS` au démarrage de l'implémentation.
